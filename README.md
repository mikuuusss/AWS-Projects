# AWS-Projects
This AWS cloud services hands gain-on practice done by me.
In this .pdf files you able to see the steps and way of thinking in each case.

Skills practiced in:
  AWS RDS Database practice: 
  -deployment an Amazon RDS  database instance on AWS
  -Configuration secure EC2-to-RDS communication using VPC Security Groups
  -Connecting to the database through a Linux EC2 instance using the MariaDB client
  -Designing relational database schemas and populated sample datasets
  -administration, SQL development, and AWS infrastructure management.

AWS Network setup (Custom VPC + Multi-Subnet Network + EC2 Web Server):
  -VPC design and IPv4 subnetting (CIDR planning) 
  -Public vs private subnet isolation and routing
  -IGW vs NAT Gateway use cases
  -Security Groups as instance-level firewalls 
  -EC2 launch configuration + user-data bootstrapping 
  -Basic Linux service setup (Apache start on boot)

AWS Lambda and SNS:
  -Amazon S3
  -AWS Lambda
  -Amazon SNS
  -Event-Driven Architecture
  -Python (Boto3)
  -S3 Event Notifications
  -Serverless Computing

  
